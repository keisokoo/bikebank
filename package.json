{
  "name": "bikebank-kr",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist && parcel build index.pug -d dist --no-minify && cp public/* dist",
    "dev": "parcel index.pug --open",
    "start-parcel": "npm run build && npm run dev"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^7.2.1",
    "es-dev-server": "^2.1.0",
    "parcel-bundler": "^1.12.5",
    "pug": "^3.0.2",
    "sass": "^1.52.2",
    "typescript": "^4.7.3"
  }
}
